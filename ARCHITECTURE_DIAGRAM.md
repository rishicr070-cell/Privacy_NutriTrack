# 🏗️ Storage Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                        NutriTrack App                        │
│                     (Flutter Frontend)                       │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   storage_helper.dart                        │
│              (Unified Storage Interface)                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  • saveFoodEntry()      • getUserProfile()             │ │
│  │  • getFoodEntries()     • setDarkMode()                │ │
│  │  • saveWaterIntake()    • clearAllData()               │ │
│  └────────────────────────────────────────────────────────┘ │
└────────┬─────────────────────────┬──────────────────────────┘
         │                         │
         ▼                         ▼
┌──────────────────┐      ┌──────────────────────┐
│  database_helper │      │ secure_storage_helper│
│    (SQLite DB)   │      │   (Encrypted)        │
└────────┬─────────┘      └──────────┬───────────┘
         │                           │
         ▼                           ▼
┌─────────────────┐       ┌──────────────────────┐
│  nutritrack.db  │       │   Keychain (iOS)     │
│  ┌───────────┐  │       │   KeyStore (Android) │
│  │ Tables:   │  │       │   ┌────────────────┐ │
│  │           │  │       │   │ dark_mode      │ │
│  │ • food_   │  │       │   │ notifications  │ │
│  │   entries │  │       │   │ first_run      │ │
│  │ • user_   │  │       │   │ biometric      │ │
│  │   profile │  │       │   └────────────────┘ │
│  │ • water_  │  │       └──────────────────────┘
│  │   intake  │  │
│  │ • weight_ │  │
│  │   log     │  │
│  └───────────┘  │
└─────────────────┘
```

## Data Flow Diagram

### Writing Data
```
User Action (e.g., Log Food)
         │
         ▼
    UI Screen
         │
         ▼
StorageHelper.saveFoodEntry(entry)
         │
         ├──────────────────┐
         ▼                  ▼
   Is Bulk Data?      Is Sensitive?
   (Food Entry)         (Theme)
         │                  │
         ▼                  ▼
  DatabaseHelper    SecureStorageHelper
         │                  │
         ▼                  ▼
    SQLite DB         Keychain/KeyStore
         │                  │
         ▼                  ▼
   Device Storage      Encrypted Storage
```

### Reading Data
```
App Startup / User Request
         │
         ▼
    UI Screen
         │
         ▼
StorageHelper.getFoodEntries()
         │
         ▼
  DatabaseHelper.getAllFoodEntries()
         │
         ▼
    SQLite Query
         │
         ▼
  Return List<FoodEntry>
         │
         ▼
    Update UI
```

## Storage Decision Tree

```
                Need to Store Data?
                        │
                        ▼
            ┌───────────┴───────────┐
            │                       │
       Is it bulk/         Is it sensitive?
    structured data?      (password, PIN)
            │                       │
            ▼                       ▼
     Use SQLite DB          Use Secure Storage
            │                       │
    ┌───────┴───────┐              │
    ▼               ▼              ▼
Food Entries    User Profile   Theme
Water Logs      Weight Logs    Settings
                               Credentials
```

## File Structure

```
lib/utils/
├── storage_helper.dart          ← You use this!
│   ├── Uses DatabaseHelper for bulk data
│   ├── Uses SecureStorageHelper for sensitive data
│   └── Provides simple interface
│
├── database_helper.dart         ← SQLite operations
│   ├── Creates database tables
│   ├── CRUD operations
│   └── Query methods
│
└── secure_storage_helper.dart   ← Encryption operations
    ├── Platform-specific security
    ├── Key management
    └── Encrypted read/write
```

## Platform-Specific Implementation

### iOS Architecture
```
┌─────────────────────────────────┐
│      Flutter App (Dart)         │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│   flutter_secure_storage        │
│   (iOS implementation)          │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│      iOS Keychain API           │
│  (kSecAttrAccessible)           │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│   Encrypted Keychain Storage    │
│   (Hardware-backed if available)│
└─────────────────────────────────┘
```

### Android Architecture
```
┌─────────────────────────────────┐
│      Flutter App (Dart)         │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│   flutter_secure_storage        │
│   (Android implementation)      │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│   Android KeyStore System       │
│   (AES Encryption)              │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│   EncryptedSharedPreferences    │
│   (Encrypted at rest)           │
└─────────────────────────────────┘
```

## Database Schema Diagram

```
┌─────────────────────────────────────────┐
│         nutritrack.db (SQLite)          │
├─────────────────────────────────────────┤
│                                         │
│  ┌────────────────────────────────┐    │
│  │      food_entries              │    │
│  ├────────────────────────────────┤    │
│  │ id (TEXT) PRIMARY KEY          │    │
│  │ foodName (TEXT)                │    │
│  │ calories (REAL)                │    │
│  │ protein (REAL)                 │    │
│  │ carbs (REAL)                   │    │
│  │ fat (REAL)                     │    │
│  │ servingSize (REAL)             │    │
│  │ servingUnit (TEXT)             │    │
│  │ mealType (TEXT)                │    │
│  │ timestamp (INTEGER)            │    │
│  └────────────────────────────────┘    │
│                                         │
│  ┌────────────────────────────────┐    │
│  │      user_profile              │    │
│  ├────────────────────────────────┤    │
│  │ id (INTEGER) PRIMARY KEY       │    │
│  │ name (TEXT)                    │    │
│  │ age (INTEGER)                  │    │
│  │ gender (TEXT)                  │    │
│  │ height (REAL)                  │    │
│  │ currentWeight (REAL)           │    │
│  │ targetWeight (REAL)            │    │
│  │ activityLevel (TEXT)           │    │
│  │ dailyCalorieGoal (REAL)        │    │
│  │ dailyProteinGoal (REAL)        │    │
│  │ dailyCarbsGoal (REAL)          │    │
│  │ dailyFatGoal (REAL)            │    │
│  │ dailyWaterGoal (REAL)          │    │
│  │ healthConditions (TEXT)        │    │
│  │ allergies (TEXT)               │    │
│  └────────────────────────────────┘    │
│                                         │
│  ┌────────────────────────────────┐    │
│  │      water_intake              │    │
│  ├────────────────────────────────┤    │
│  │ date (TEXT) PRIMARY KEY        │    │
│  │ amount (INTEGER)               │    │
│  └────────────────────────────────┘    │
│                                         │
│  ┌────────────────────────────────┐    │
│  │      weight_log                │    │
│  ├────────────────────────────────┤    │
│  │ date (TEXT) PRIMARY KEY        │    │
│  │ weight (REAL)                  │    │
│  └────────────────────────────────┘    │
│                                         │
└─────────────────────────────────────────┘
```

## Security Layers

```
┌─────────────────────────────────────────────┐
│            Application Layer                │
│  (Flutter App - No sensitive data in code)  │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│         Storage Abstraction Layer           │
│     (storage_helper.dart - Routes data)     │
└──────────┬──────────────────┬───────────────┘
           │                  │
           ▼                  ▼
┌──────────────────┐  ┌──────────────────────┐
│  Database Layer  │  │ Secure Storage Layer │
│  (SQLite - No    │  │ (Platform Encrypted) │
│   Encryption)    │  │                      │
└──────────┬───────┘  └──────────┬───────────┘
           │                     │
           ▼                     ▼
┌──────────────────┐  ┌──────────────────────┐
│  Device Storage  │  │  OS Security Layer   │
│  (File System)   │  │  Keychain/KeyStore   │
└──────────────────┘  └──────────────────────┘
           │                     │
           └──────────┬──────────┘
                      ▼
        ┌───────────────────────┐
        │   Physical Device     │
        │   (Encrypted Storage) │
        └───────────────────────┘
```

## Data Lifecycle

### Create
```
User Input → Validation → Storage Helper → Database/Secure Storage → Disk
```

### Read
```
App Request → Storage Helper → Query Database/Read Secure → Parse → Return to UI
```

### Update
```
User Edit → Validation → Storage Helper → Update in DB/Storage → Refresh UI
```

### Delete
```
User Delete → Confirmation → Storage Helper → Remove from DB/Storage → Update UI
```

## Performance Optimization

```
Storage Helper
      │
      ├─── In-Memory Cache (Future Enhancement)
      │     └─── Recently accessed data
      │
      ├─── Lazy Loading
      │     └─── Load data only when needed
      │
      └─── Batch Operations
            └─── Group writes for efficiency
```

## Error Handling Flow

```
Operation Attempt
      │
      ▼
   Try Block
      │
      ├─── Success → Return Data
      │
      └─── Exception
            │
            ▼
       Catch Block
            │
            ├─── Log Error
            ├─── Return Default
            └─── Show User Message
```

## Backup & Recovery (Future)

```
┌─────────────────────────────────────┐
│         Backup Strategy             │
├─────────────────────────────────────┤
│                                     │
│  Local Database                     │
│       │                             │
│       ▼                             │
│  Export to JSON/CSV                 │
│       │                             │
│       ▼                             │
│  Save to Documents                  │
│       │                             │
│       ▼                             │
│  [Optional] Upload to Cloud         │
│  (Encrypted, User Controlled)       │
│                                     │
└─────────────────────────────────────┘
```

---

## Summary

**Three Storage Systems:**
1. **SQLite** - Bulk structured data
2. **Secure Storage** - Sensitive encrypted data  
3. **SharedPreferences** - Simple config

**One Interface:**
- `storage_helper.dart` - All your app needs

**Zero Changes:**
- Your existing code works as-is
- Data automatically routes to correct storage

**Better Everything:**
- ✅ Performance
- ✅ Security
- ✅ Scalability
- ✅ Privacy

---

*This diagram represents the complete storage architecture of NutriTrack v1.0*
